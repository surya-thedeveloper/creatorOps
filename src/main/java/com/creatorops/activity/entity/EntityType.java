package com.creatorops.activity.entity;

/**
 * Defines the types of entities that can be associated with an activity log record.
 */
public enum EntityType {
    CONTENT,
    RESEARCH,
    SCRIPT,
    ASSIGNMENT,
    TASK,
    ASSET
}
