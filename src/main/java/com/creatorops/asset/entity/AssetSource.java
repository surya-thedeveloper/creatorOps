package com.creatorops.asset.entity;

/**
 * Defines the storage sources/platforms from which an asset is referenced.
 */
public enum AssetSource {
    GOOGLE_DRIVE,
    ONEDRIVE,
    EXTERNAL_URL,
    LOCAL_UPLOAD
}
